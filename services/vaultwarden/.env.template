# Vaultwarden Configuration
# Copy this file to .env and set a secure admin token

# Generate a secure token with: openssl rand -base64 48
VAULTWARDEN_ADMIN_TOKEN=your_secure_admin_token_here

# Optional: Configure SMTP for password reset emails
# SMTP_HOST=smtp.gmail.com
# SMTP_FROM=your-email@gmail.com
# SMTP_PORT=587
# SMTP_SECURITY=starttls
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password